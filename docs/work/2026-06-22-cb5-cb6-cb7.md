# CB.5 + CB.6 + CB.7 — Ball Glyph Grammar, Bloom Interference, Platformer Cleanup

**Date:** 2026-06-22
**Branch:** `claude/handoff-review-cb-tasks-vv5var`
**Roadmap items:** Phase CB — CB.5, CB.6, CB.7

## Goal

One PR delivers all three remaining Core Breaker slices: (1) ball glyph grammar + dual-face module surface info in the HUD and Workbench, (2) Bloom interference soft-fail that drains banked yield if growths are ignored, (3) full platformer code removal.

## Approach

### CB.5 — Ball glyph grammar + dual-face module surface info

The GDD §6.4 says each ball reads at a glance via a trajectory glyph (straight/arc/curve) plus a count badge, with no text required. A module should show both its combat face (cards) and its surface face (ball glyph + type) on one card at 375px.

Ball → glyph mapping (§6.4 UI law):
- `drill` → **straight** (bores through — pierce trajectory)
- `standard`, `heavy`, `split` → **arc** (bouncy)
- `ghost` → **curve** (phases through, gently curving)

Implementation plan:
- Add `BALL_GLYPH: Record<BallType, 'straight' | 'arc' | 'curve'>` to `src/game/data/core-breaker.ts`
- Add `ballFace: { type: BallType; glyph: 'straight' | 'arc' | 'curve' } | null` to `ShipModuleView` in `ship-view.ts`; populate it from `projectMiningRoster` in `buildShipView`
- Create `src/components/foundry/BallGlyphIcon.tsx` — CSS-drawn SVG glyphs for straight/arc/curve + label
- Update Workbench `ModuleRow`: add a compact "surface face" row (glyph + ball type label) below the toggle area
- Update `CoreBreakerHud`: add a small count badge overlay (`×N`) on each ball thumbnail in the queue preview

### CB.6 — Bloom interference (soft-fail)

GDD §6.5 / §6.10: "a growth that must be cleared with a shot, or it starts eating banked yield."

The drain lives at the **renderer layer** (not the sim) because it persists across shots and is not determinism-critical. The sim stays untouched.

- Add tunable constants `BLOOM_DRAIN_THRESHOLD = 3` and `BLOOM_DRAIN_PER_SHOT = 1` to `src/game/data/mining-run.ts`
- In `core-breaker-renderer.ts`: track `bloomShotsActive` (reset when zero bloom pegs remain). After the threshold, subtract `BLOOM_DRAIN_PER_SHOT` Scrap from `banked.scrap` at the end of each shot. Clamp at 0.
- Add `bloomThreat: number` (integer shots-over-threshold, 0 = safe) to `CoreBreakerHudState`; emit `onHud` when it changes
- HUD header: a compact amber chip "BLOOM ▲N" that appears when `bloomThreat > 0`, fades when 0
- Tests: at least 3 covering threshold behaviour, yield drain, and clear-resets

### CB.7 — Platformer cleanup

Delete all retired platformer source + tests and wire out the `?mode=surface` path. `?mode=surface` becomes an alias for `?mode=mining`.

**Files to delete:**
```
src/game/surface/physics.ts       physics.test.ts
src/game/surface/clone.ts         clone.test.ts
src/game/surface/pod.ts           pod.test.ts
src/game/surface/surface.ts       surface.test.ts
src/game/surface/tilemap.ts       tilemap.test.ts
src/game/surface/items.ts         items.test.ts
src/game/surface/mining.ts        mining.test.ts
src/game/surface/drops.ts         drops.test.ts
src/game/surface/hazards.ts       hazards.test.ts
src/game/surface/enemies.ts       enemies.test.ts
src/game/surface/clone-combat.test.ts
src/renderer/surface-renderer.ts
src/game/modes/surface-mode.ts
src/game/surface-view.ts          surface-view.test.ts
src/components/SurfaceHUD.tsx
src/components/TouchControls.tsx
```

**`main.ts` surgery:**
- Remove imports of `startSurfaceMode`, `SurfaceMode`, `SurfaceAction`, `projectLoadout` (surface items), `SurfaceView`
- Remove `surfaceMode: SurfaceMode | null` variable
- Remove `resolveDevSurface()` function; alias `?mode=surface` → `?mode=mining` in `resolveMiningMode`
- Remove `enterSurface()` function
- Remove `onSurfaceUpdate?` from `GameCallbacks`
- Remove `surfaceInput`, `launchPod` (old), `reprintClone` (old) from `GameHandle`
- Remove `'surface'` from `GamePhase`

**`page.tsx` surgery:**
- Remove `phase === 'surface'` block (~80 lines)
- Remove `surfaceView` state and `onSurfaceUpdate` callback
- Remove `hasDash` calculation
- Remove `SurfaceHUD` and `TouchControls` imports

**`GameCanvas.tsx` surgery:** remove `onSurfaceUpdate` wiring.

## Steps

### CB.5
- [ ] Add `BALL_GLYPH` const to `src/game/data/core-breaker.ts`
- [ ] Add `ballFace` to `ShipModuleView` (ship-view.ts); populate in `buildShipView`
- [ ] Create `src/components/foundry/BallGlyphIcon.tsx`
- [ ] Update Workbench `ModuleRow` to show ball face
- [ ] Update `CoreBreakerHud` queue items with count badge overlay

### CB.6
- [ ] Add drain constants to `src/game/data/mining-run.ts`
- [ ] Add bloom drain logic to `core-breaker-renderer.ts`; add `bloomThreat` to `CoreBreakerHudState`
- [ ] Add BLOOM chip to `CoreBreakerHud` header
- [ ] Write 3 bloom-drain tests in `core-breaker-renderer.test.ts` (or new file)

### CB.7
- [ ] `git rm` all 28 retired files
- [ ] `main.ts` surgery (remove surface path, alias `?mode=surface`)
- [ ] `page.tsx` surgery (remove surface phase block)
- [ ] `GameCanvas.tsx` surgery (remove `onSurfaceUpdate`)
- [ ] Remove `'surface'` from `GamePhase` + re-export cleanup
- [ ] `pnpm type-check` + `pnpm test` — must be green before commit

## Manual test steps

- [ ] `localhost:3000?mode=mining` at 375px: HUD ball queue shows count badges; no JS errors
- [ ] Open Workbench in a run: each installed module shows its ball glyph + type below the card list
- [ ] Fire 4+ shots with a bloom peg alive: Scrap count decreases, "BLOOM ▲N" chip appears in header
- [ ] Shoot the bloom peg: chip disappears, drain stops
- [ ] `localhost:3000?mode=surface`: loads mining run (not the old platformer)
- [ ] Full run start → planet → orbit → DROP → mining: works end-to-end
- [ ] Edge: all-drill loadout — straight glyphs shown everywhere; no arc/curve glyphs

## Out of scope for this session

- Juice FX on bloom drain (screen tint, drain number floater)
- Live bounce-countdown pips (cosmetic, v2 sim has no bounce budget)
- Orbit→mining orientation snap smooth-out
- HUD band alignment on landscape desktop
- `data/surface.ts` platformer tuning constants (kept for now — they're referenced by tests; remove when no longer needed)

---

## What actually happened

All three slices landed cleanly in one session.

**CB.5** — `BALL_GLYPH` map added to `data/core-breaker.ts`. `ballFace` added to both `ShipModuleView` and `CargoModuleView` in `ship-view.ts`, populated via a `modulesBallFaces()` helper that calls `projectMiningRoster`. `BallGlyphIcon.tsx` created with inline SVG for straight/arc/curve. Workbench `ModuleRow` updated to show a compact surface face chip. `CoreBreakerHud` shows glyph next to the armed ball name and count badges (`×N`) on queue previews.

**CB.6** — `BLOOM_DRAIN_THRESHOLD` and `BLOOM_DRAIN_PER_SHOT` constants added to `mining-run.ts`. Extracted `calcBloomDrain()` as a pure function (testable) and had the renderer call it. `bloomThreat` added to `CoreBreakerHudState`; BLOOM chip (`BLOOM ▲N`, pulsing red) added to the HUD header. 5 unit tests in `mining-run.test.ts` (threshold, drain, clamp, reset, per-shot amount).

**CB.7** — 27 platformer files deleted. `main.ts` stripped of all surface path code (`startSurfaceMode`, `enterSurface`, `SurfaceView`, `onSurfaceUpdate`, surface from `GamePhase`); `?mode=surface` aliased to `?mode=mining`. `page.tsx` stripped of the ~80-line surface phase block, `SurfaceView` type reference, and unused state. `GameCanvas.tsx` stripped of `onSurfaceUpdate` prop. Stale `skyRampFor`/`surfaceRampFor` import removed from `main.ts`.

387 tests green, type-check clean, lint clean.

## Files created / modified

- `src/game/data/core-breaker.ts` — `BallGlyph` type + `BALL_GLYPH` map
- `src/game/ship-view.ts` — `ballFace` on both view types, `modulesBallFaces()` helper
- `src/components/foundry/BallGlyphIcon.tsx` — new component
- `src/components/Workbench.tsx` — surface face chip in `ModuleRow`
- `src/components/CoreBreakerHud.tsx` — glyph in armed row, count badges, bloom chip
- `src/game/data/mining-run.ts` — drain constants + `calcBloomDrain()` pure function
- `src/renderer/core-breaker-renderer.ts` — bloom drain, `bloomThreat` in HUD state
- `src/game/data/mining-run.test.ts` — 5 bloom drain tests (new file)
- `src/game/main.ts` — surface path removed, palette import removed
- `src/app/page.tsx` — surface phase block removed, type refs fixed
- `src/components/GameCanvas.tsx` — `onSurfaceUpdate` removed
- 27 platformer source + test files deleted via `git rm`

## Deferred to next session

- Juice FX on bloom drain (screen tint, drain number floater)
- HUD band alignment on landscape desktop (minor cosmetic)
- `data/surface.ts` tuning constants (kept — no longer imported, harmless)

## Status

- [x] Complete
